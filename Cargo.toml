[package]
name = "dist-test"
version = "0.4.4"
edition = "2024"
repository = "https://github.com/tofoss/cargo-dist-test"

[dependencies]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
